# Brakeman Configuration for Rails 8 Project

# Exit behavior
exit_on_warn: false
exit_on_error: false

# Rails version detection
rails_version: 8

# Scan options
skip_checks:
  - "CheckDeserialize"
  - "CheckFileAccess"

# Performance options
parallel_checks: true
quiet: false
